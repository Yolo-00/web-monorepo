engine-strict=true       # 强制使用 package.json 中指定的 Node.js 版本
shamefully-hoist=true    # 将依赖提升到 node_modules，解决幽灵依赖问题

# 依赖检查配置
strict-peer-dependencies=false   # 是否严格检查对等依赖
auto-install-peers=true         # 自动安装对等依赖